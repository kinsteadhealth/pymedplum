{
  "name": "@pymedplum/generator",
  "version": "1.0.0",
  "private": true,
  "description": "TypeScript to Pydantic code generator for PyMedplum",
  "scripts": {
    "generate": "ts-node src/index.ts",
    "build": "tsc",
    "clean": "rm -rf ../pymedplum/fhir/*.py",
    "test": "ts-node src/test.ts"
  },
  "dependencies": {
    "@medplum/fhirtypes": "^5.0.10",
    "@types/node": "^20.0.0",
    "prettier": "^3.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0"
  },
  "devDependencies": {
    "@types/prettier": "^3.0.0"
  }
}
